version: '3.8'

services:
  ai-roundtable:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - ./tts_output:/app/tts_output
    restart: unless-stopped
